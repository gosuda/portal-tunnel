# Build-time manifest, embedded into the binary via go:embed.
# Bump protocol versions only when wire-level behavior changes.

[release]
version  = "v2.2.3"
base_url = "https://github.com/gosuda/portal-tunnel/releases"

[protocol]
tunnel    = "7"
discovery = "7"
